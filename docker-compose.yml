version: '3'

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    env_file:
      - ./frontend/.env

  backend:
    build:
      context: ./backend
    ports:
      - "8000:80"
    env_file:
      - ./backend/.env